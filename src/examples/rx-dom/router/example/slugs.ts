import { IRXRouteSlug } from '../slugs/create-slugs';

export const SLUGS: IRXRouteSlug[] = [
  ['/home', ['/accueil']],
  ['/product/:productId', ['/produit/:productId']],
  ['/list', ['/liste']],
];

